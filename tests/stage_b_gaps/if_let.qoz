// expect: 42

import option

let unwrap_or(o: Option<i64>, def: i64): i64 = {
    if let Some(v) = o {
        v
    } else {
        def
    }
}

let main(): i64 = unwrap_or(Some(42), 0) + unwrap_or(None, 0)
