// expect: 5
// Generic ADT Option<T> with match and inferred unwrap_or.

import option

let unwrap_or<T>(o: Option<T>, fallback: T): T = match o {
| Some(x) -> x
| None    -> fallback
}

let main(): i64 = {
    let some: Option<i64> = Some(5)
    let none: Option<i64> = None
    unwrap_or(some, 0) + unwrap_or(none, 0)
}
