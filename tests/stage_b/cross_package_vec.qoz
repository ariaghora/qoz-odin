// expect: 6
// std/vec end-to-end: push three i64, index back.

import vec

let main(): i64 = {
    var v: Vec<i64> = vec.make()
    vec.push(&v, 1)
    vec.push(&v, 2)
    vec.push(&v, 3)
    v.data[0] + v.data[1] + v.data[2]
}
