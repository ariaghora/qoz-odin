import strings
import fmt
import vec

let main() = {
    let xs: Vec<i32> = [11, 22, 33, 44]
    for x in xs {
        fmt.println("{x}")
    }

    let names: Vec<string> = ["alice", "bob", "carol"]
    for n in names {
        fmt.println(n)
    }
}
