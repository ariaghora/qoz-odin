import std/fmt

type Shape =
    | Circle(f64)
    | Square(f64)
    | Triangle(f64, f64, f64)

let describe(s: Shape): string =
    match s {
    | Shape.Circle(r) -> "circle"
    | _               -> "other"
    }

let main() = {
    fmt.println(describe(Shape.Square(2.0)))
}
