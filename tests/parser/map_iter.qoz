import strings
import fmt
import map

let main() = {
    var m: Map<string, i64> = map.make()
    m["one"]   = 1
    m["two"]   = 2
    m["three"] = 3

    var total: i64 = 0
    for k, v in m {
        total = total + v
    }
    fmt.println(`total = {total}`)
}
