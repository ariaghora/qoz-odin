import strings
import fmt

type Point = { x: i32, y: i32 }

let main() = {
    var p = Point { x: 3, y: 4 }
    let pp: *Point = &p
    fmt.println("p.x = {p.x}")
    fmt.println("pp.x = {pp.x}")
    fmt.println("pp.y = {pp.y}")
}
