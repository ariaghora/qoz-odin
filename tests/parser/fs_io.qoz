import strings
import fmt
import fs

let main() = {
    let ok = fs.write_file("/tmp/qoz_fs_io_test.txt", "hello qoz\n")
    fmt.println(`write ok = {ok}`)
    let back = fs.read_file("/tmp/qoz_fs_io_test.txt")
    fmt.println(`read = {back}`)
    fmt.println("len = {len(back)}")
}
