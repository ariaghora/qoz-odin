import std/fmt
import std/vec

let main() = {
    var nums: Vec<i64> = vec.make()
    vec.push(&nums, 10)
    vec.push(&nums, 20)
    vec.push(&nums, 30)

    var sum: i64 = 0
    for n in nums {
        sum = sum + n
    }
    fmt.println("sum:", sum)
}
