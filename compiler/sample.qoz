import std/fmt

let main() = {
    var total: i64 = 0
    for i in 0..<5 {
        total = total + i
    }
    fmt.println("range sum:", total)

    var inclusive: i64 = 0
    for i in 1..=4 {
        inclusive = inclusive + i
    }
    fmt.println("inclusive sum:", inclusive)
}
