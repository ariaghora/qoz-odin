malloc := func(size: i32): *void external

Vec2D := struct {
    x: i32,
    y: i32,
}

Foo := struct {
    position: Foo,
}

Bar := struct {
    position: Bar,
}

Node := struct {
    value: Vec2D,
}

main := func() {
    pos := Vec2D { 
        x: 10,
        y: 20,
    }

    root := Node {
        value: pos,
    }

    print(root.value.x)
}