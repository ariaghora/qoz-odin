import "std:strings"
import "./math"

main := func() {
    word1 := "Hello, "
    word2 := "World!"
    greeting := strings.string_concat(word1, word2)
    print(greeting)

    x := math.add(3, 4)
    print(x)
}