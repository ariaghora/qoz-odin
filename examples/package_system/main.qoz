import "./math"
import str "std:strings" // Import can be aliased

main := func() {
    alloc := mem.default_allocator()

    word1 := "Hello, "
    word2 := "World!"
    greeting := str.string_concat(word1, word2, alloc)
    print(greeting)

    x := math.add(3, 4)
    print(x)

    del(greeting, alloc)
}