main := func() {
    numbers := arr<i32, 5>{1, 2, 3, 4, 5}
    
    sum : i32 = 10
    for n in numbers {
        sum = sum + n
    }

    print(sum)

    print(len(numbers))

    numbers[2] = 42
    for n in numbers {
        print(n)
    }
}
